# Example env for NannyChain backend
# Copy to .env and fill values.

# ============ Required for current features ============
# PostgreSQL connection string
DATABASE_URL=postgresql://nannychain:password@db:5432/nannychain

# Redis (USSD session storage)
REDIS_URL=redis://redis:6379/0

# Used to encrypt Stellar secret keys in DB. Use a long random string in production.
ENCRYPTION_KEY=changeme

# ============ Stellar ============
# TESTNET or PUBLIC
STELLAR_NETWORK=TESTNET
# Optional: secret key of account that funds new testnet wallets with 1 XLM
STELLAR_FUNDING_SECRET=

# ============ Africa's Talking (SMS + USSD) ============
AT_USERNAME=
AT_API_KEY=

# Optional: if set, POST /ussd must send header: Authorization: Bearer <USSD_API_KEY>
USSD_API_KEY=

# USSD session TTL in seconds (default 180)
USSD_SESSION_TTL=180

# ============ M-Pesa (planned) ============
MPESA_CONSUMER_KEY=
MPESA_CONSUMER_SECRET=
MPESA_PASSKEY=

# ============ App ============
SECRET_KEY=changeme
