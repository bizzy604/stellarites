name: Deploy Backend to Render

on:
  push:
    branches:
      - main
    paths:
      - 'Backend/**'
      - 'render.yaml'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Deploy to Render
      run: |
        # Render deployments are triggered automatically when you push to the connected branch
        # This workflow ensures the code is pushed, but actual deployment happens on Render's side
        echo "Code pushed to main branch. Render will deploy automatically if connected."